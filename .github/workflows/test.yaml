name: Test docker on github
on: [push]
jobs:
        Test-Docker:
                runs-on: ubuntu-latest
                steps:
                        - uses: actions/checkout@v3
                        - run: make pull
                        - run: make test
                        
